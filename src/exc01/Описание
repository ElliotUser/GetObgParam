Объект передается методу в качесве параметра.
Метод equals ( ) проверяет в классе Test на равенство два объекта и
возвращает получаемый результат. Таким образом, он сравнивает вызывающий
объект с тем, который бьл ему передан. Если оба объекта содержат одинаковые
значения, метод equals ( ) возвращает логическое значение true, а иначе - логи-
ческое значение false.