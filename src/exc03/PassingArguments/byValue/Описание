Передача аргумента.
Вызов ПО ЗНАЧЕНИЮ.
Когда методу передается аргумент примитивного типа, его
передача происходит по значению. В итоге создается копия
аргумента, и все, что происходит с параметром, принимающим
этот аргумент, не оказывает никакого влияния за пределами
вызываемого метода.
В данном примере операции, выполняемые в теле метода meth (), не оказывают
никакого влияния на значения переменных а и Ь, используемых
при вызове этого метода.